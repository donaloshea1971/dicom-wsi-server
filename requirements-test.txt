# Test Dependencies for DICOM WSI Server
# 
# NOTE: Tests also require the main application dependencies from converter/requirements.txt
# At minimum, install these before running tests:
#   pip install python-jose[cryptography] asyncpg pydantic-settings fastapi

# Testing framework
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0

# Async testing
anyio>=4.2.0

# Mocking and test utilities
responses>=0.24.0
freezegun>=1.4.0

# Code quality (optional but recommended)
coverage>=7.4.0

# HTTP mocking
respx>=0.20.2

# FastAPI testing
httpx>=0.26.0
starlette>=0.35.0

# For TIFF compression tests (optional)
tifffile>=2024.1.0
# imagecodecs>=2024.1.0  # Optional: enables LZW compression tests
